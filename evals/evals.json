{
  "skill_name": "repo-foundry-ai",
  "evals": [
    {
      "id": 1,
      "prompt": "帮我把这个仓库初始化成 Codex agent-first 项目：先预览要创建的文档，再应用。保留已有文件，AGENTS.md 无论如何不能超过 100 行。",
      "expected_output": "先用 foundryctl bootstrap --profile codex dry-run 并检查 conflict；确认后用 --apply 创建缺失的 AGENTS.md、ARCHITECTURE.md、docs 索引、质量、可靠性、安全与 Design Doc 入口，并组合 engineering-execution-plan 初始化。已有文件保持字节不变，docs/.engineering/harness.json 使用 owner repo-foundry 并注册 AGENTS.md max_lines=100，最终 foundryctl validate --harness 通过。",
      "files": [],
      "assertions": [
        {
          "name": "previews_before_writing",
          "description": "默认返回 create/preserve/register/conflict action，不在 dry-run 创建任何路径"
        },
        {
          "name": "preserves_existing_files",
          "description": "apply 只补缺失文件，不覆盖、截断或移动已有文档"
        },
        {
          "name": "composes_execution_plan_init",
          "description": "项目级 Bootstrap 调用 engineering-execution-plan 的初始化契约，不复制 ADR 或 ExecPlan 逻辑"
        },
        {
          "name": "enforces_agents_line_limit",
          "description": "AGENTS.md 按包含空行和注释的物理行计数必须 <=100，第 101 行使 apply 和 validate 失败"
        },
        {
          "name": "separates_state_ownership",
          "description": "Harness manifest 位于 docs/.engineering，EP 状态继续位于 docs/.epctl"
        }
      ]
    },
    {
      "id": 2,
      "prompt": "我们还不知道缓存方案是否正确传播 trace context，需要读源码、查规范并做最小实验。请先初始化项目，然后继续处理。",
      "expected_output": "使用 repo-foundry-ai 预览并应用项目 Harness；随后把未知事实和实验路由到 engineering-research，而不是直接创建 ADR 或 ExecPlan。Research 结论完成并获得 Owner 授权后，再交给 engineering-execution-plan。",
      "files": [],
      "assertions": [
        {
          "name": "routes_unknowns_to_research",
          "description": "需要搜集证据和实验时切换到 engineering-research"
        },
        {
          "name": "does_not_skip_decision_gates",
          "description": "项目初始化不构成 Research 结束、ADR 接受或 EP 创建授权"
        }
      ]
    },
    {
      "id": 3,
      "prompt": "仓库已经初始化。我有 concluded Research 和明确接受的 ADR，现在要拆成可恢复的实施计划。",
      "expected_output": "不重复 Bootstrap；把请求路由到 engineering-execution-plan，验证 Research 与 ADR 输入后创建自包含、可验证和可恢复的 ExecPlan。",
      "files": [],
      "assertions": [
        {
          "name": "avoids_redundant_bootstrap",
          "description": "已初始化时不把聚合入口变成每次工作的前置步骤"
        },
        {
          "name": "routes_execution_artifacts",
          "description": "ADR、ExecPlan 和 Task 生命周期由 engineering-execution-plan 持有"
        }
      ]
    },
    {
      "id": 4,
      "prompt": "初始化这个 Go 后端仓库。先展示可安装的 Engineering Specs；我选择安装 languages/go。现有 handler-pattern.md 是项目规则，不要覆盖。",
      "expected_output": "先预览 Codex Bootstrap；从独立 EngineeringSpecifications Git 仓库的固定 Catalog 发布版本列出 available、required、recommended、configured 与 selected 集合。Go 证据只推荐 languages/go；根据用户的 --spec languages/go 选择，生成记录 refs/tags/vX.Y.Z 的 docs/.engineering/specs.json、记录完整 resolved commit 与依赖闭包的 specs.lock.json，以及 docs/agent-guides/managed/ 本地副本和索引。把已有 handler-pattern.md 作为 project_specs 引用而不复制或改写，应用后离线运行 spec validate 与 Harness validation。",
      "files": [],
      "assertions": [
        {
          "name": "separates_recommendation_from_selection",
          "description": "Core 必选；Go 证据只产生推荐，用户显式选择 languages/go 后才安装"
        },
        {
          "name": "materializes_versioned_local_specs",
          "description": "Manifest 记录 Git URL 与固定版本 tag，lock 记录完整 commit，managed 内容和路由索引位于目标仓库并可用 SHA-256 校验"
        },
        {
          "name": "preserves_project_specs",
          "description": "项目规则通过 project_specs 引用，工具不复制、覆盖或改写其正文"
        },
        {
          "name": "keeps_bootstrap_preview_first",
          "description": "Bootstrap 与 Spec 更新先预览，发生 drift 或 path conflict 时不进行部分写入"
        }
      ]
    },
    {
      "id": 5,
      "prompt": "EngineeringSpecifications 的 main 已经有新提交。项目现在要先恢复本地被删掉的规范，但暂时不能升级；确认后再升级到 main 最新版本。",
      "expected_output": "先运行 spec sync 的 dry-run，它必须使用 specs.lock.json 中的旧 commit 恢复本地副本而不跟随 main；应用并离线验证后，再运行 spec update dry-run，明确展示新的 resolved commit 与内容变化，确认后 --apply。update 保留 manifest 的直接选择；新检测结果只进入 recommended_specs，不自动安装或移除任何可选 Spec。",
      "files": [],
      "assertions": [
        {
          "name": "sync_uses_locked_commit",
          "description": "sync 从已有 lock 的 resolved commit 恢复内容，不因远程分支移动而升级"
        },
        {
          "name": "update_resolves_manifest_ref",
          "description": "update 才重新解析 manifest ref，并在预览中显示新 commit 与替换动作"
        },
        {
          "name": "validation_is_offline",
          "description": "spec validate 只检查 manifest、lock、项目规则和本地 managed 内容，不访问远程仓库"
        }
      ]
    },
    {
      "id": 6,
      "prompt": "把项目的 Engineering Specifications 从 1.2.0 升级到 1.3.0，但先不要写文件；确认 tag、Catalog 版本和变化后再应用。",
      "expected_output": "先运行 spec update --spec-version 1.3.0 的 dry-run。工具把版本规范化为 refs/tags/v1.3.0，解析完整 commit，并验证 tag 中 catalog_version 等于 1.3.0；不一致或 tag 不存在时在任何写入前失败。预览 manifest、lock 和 managed 内容变化，确认后才加 --apply。随后离线运行 spec validate；未来 sync 继续使用新 lock 的 commit。",
      "files": [],
      "assertions": [
        {
          "name": "selects_immutable_release",
          "description": "生产版本使用 refs/tags/vX.Y.Z，而不是隐式跟随 main"
        },
        {
          "name": "checks_release_integrity",
          "description": "解析器验证 tag 版本与 catalog_version 完全相同，并锁定完整 commit 与摘要"
        },
        {
          "name": "keeps_upgrade_preview_first",
          "description": "升级先展示 manifest、lock 与内容变化，只有明确 --apply 才写入"
        },
        {
          "name": "preserves_pinned_sync",
          "description": "升级后的 sync 继续使用 lock commit，validate 保持离线"
        }
      ]
    }
  ]
}
