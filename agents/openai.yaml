interface:
  display_name: "RepoFoundry AI"
  short_description: "Bootstrap an AI-ready repo and task Spec router"
  default_prompt: "Use $repo-foundry-ai to inventory the repository, preview its Harness scaffold and available Engineering Specs, apply the user-selected Spec set, generate the project $engineering-specs Router and reviewed Codex Hooks, validate the result, and route follow-up work."
