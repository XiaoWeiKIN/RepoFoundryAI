interface:
  display_name: "RepoFoundry AI"
  short_description: "Turn any repo into an AI-ready engineering system"
  default_prompt: "Use $repo-foundry to inventory the repository, preview and apply its Harness scaffold, fetch and lock applicable Engineering Specs, validate the result, and route follow-up work."
