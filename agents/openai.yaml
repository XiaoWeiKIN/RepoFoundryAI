interface:
  display_name: "RepoFoundry AI"
  short_description: "Turn any repo into an AI-ready engineering system"
  default_prompt: "Use $repo-foundry-ai to inventory the repository, preview its Harness scaffold and available Engineering Specs, apply the user-selected Spec set, validate the result, and route follow-up work."
